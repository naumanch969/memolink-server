export enum MediaSource {
    WHATSAPP = 'whatsapp',
    WEB = 'web',
    MOBILE = 'mobile',
    DESKTOP = 'desktop',
    EXTENSION = 'extension',
    SYSTEM = 'system'
}

export enum MediaStatus {
    UPLOADING = 'uploading',
    PROCESSING = 'processing',
    READY = 'ready',
    FAILED = 'failed'
}

export enum MediaProcessingType {
    AUDIO = 'audio',
    IMAGE = 'image',
    VIDEO = 'video',
    DOCUMENT = 'document'
}
