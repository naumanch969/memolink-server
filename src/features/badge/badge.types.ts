
/**
 * Badge Enums and Shared Types
 */

export enum BadgeRarity {
    COMMON = 'common',
    UNCOMMON = 'uncommon',
    RARE = 'rare',
    EPIC = 'epic',
    LEGENDARY = 'legendary',
}

export enum BadgeCategory {
    IDENTITY = 'identity',
    MILESTONE = 'milestone',
    INSIGHT = 'insight',
    LOYALTY = 'loyalty',
    POWER = 'power',
}

export enum BadgeStatus {
    LIVE = 'live',
    LOCKED = 'locked',
    EXPIRED = 'expired',
}
